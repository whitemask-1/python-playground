I have all the required outputs and set up a dataclass to normalize user preferences

Next I need a function for creating and storing those user preferences in a config file

Then I need a function to use a specific configuration from the user preferences
