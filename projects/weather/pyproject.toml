[project]
name = "weather"
version = "0.1.0"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88
lint.select = ["E", "F", "I", "UP", "SIM"]

[tool.mypy]
strict = true

[[tool.mypy.overrides]]
module = "test.*"
ignore_errors = true
